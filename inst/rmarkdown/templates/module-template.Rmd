## module-code module-name

**Module effect** (as described above): 
**`r norman::print_module_effect("module-code")` marks**

\bigskip

```{r stemplot-module-code}
stemplot <- aplpack::stem.leaf(module_marks$module-code, unit = 1, m = 1, Min = 0, Max = 100,
                      trim.outliers = FALSE, na.rm = TRUE, printresult = FALSE)
cat(stemplot$display, sep = "\n")
```

\bigskip\bigskip

```{r sevennum-module-code}
knitr::kable(summaries["module-code",,drop = FALSE])
```

\smallskip
Fraction of marks in each degree-class range (%):

```{r classes-module-code}
knitr::kable(classes["module-code",,drop = FALSE])
```

\bigskip

```{r scatter-module-code, out.width = '100%', fig.align='center'}
norman::scatter("module-code")
```
